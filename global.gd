extends Node
## Global runtime flags, autoloaded as "Global" (see project.godot [autoload]).
## Access anywhere as `Global.DEBUG`.


## Master debug switch. When true, debug-only tools — currently the title-screen
## Gallery (art asset viewer) — are enabled. Set to false for release builds.
var DEBUG: bool = true
